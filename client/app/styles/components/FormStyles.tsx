import { StyleSheet } from 'react-native'

const formStyleObj: any = {
    sizing: {
        width: "100%"
    },
    spacing: {
        gap: 8,
    },
}

const FormStyles = StyleSheet.create(formStyleObj);
export default FormStyles;