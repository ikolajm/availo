import { StyleSheet } from "react-native";

const iconObj: any = {};
iconObj.sizing = {};

iconObj.sizing = {
    xs: "10px",
    small: "14px",
    medium: "16px",
    large: "24px",
}

const Icons = StyleSheet.create(iconObj);
export default Icons;